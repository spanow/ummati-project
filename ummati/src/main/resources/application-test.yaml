spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///ummati_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  mail:
    host: localhost
    port: 3025

logging:
  level:
    orga.takwa.ummati: DEBUG
    org.hibernate.SQL: DEBUG

